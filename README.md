
* **`decision_making.py`**: The core decision engine. It evaluates detected objects (from YOLO and OpenCV) and their estimated distances to decide on speed control (e.g., "STOP", "SLOW DOWN", "MOVE FORWARD"). It also calculates steering commands based on lane centering offsets.
